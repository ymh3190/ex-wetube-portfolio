extends layouts/main.pug

block content 
    .videocontainer
        each video in videos.reverse()
            a(href=`/videos/${video.id}`) 
                video(src=video.path width="270" height="150" style="object-fit: cover;") 
